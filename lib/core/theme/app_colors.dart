import 'package:flutter/material.dart';

class AppColors {
  // Primary palette (from prototype Tailwind config: #2196F3)
  static const Color primary = Color(0xFF2196F3);

  // Navy - deep blue for text/bg
  static const Color navy = Color(0xFF0D47A1);

  // Amber - warning/highlight
  static const Color amber = Color(0xFFF59E0B);

  // Warm - light background
  static const Color warm = Color(0xFFE3F2FD);

  // Surface
  static const Color surface = Color(0xFFFFFFFF);

  // Dark mode
  static const Color darkBackground = Color(0xFF09090B);
  static const Color darkSurface = Color(0xFF1E1E2E);
  static const Color darkCard = Color(0xFF18181B);

  // Status colors
  static const Color success = Color(0xFF22C55E);
  static const Color danger = Color(0xFFEF4444);

  // Text
  static const Color textLight = navy;
  static const Color textDark = Colors.white;
}
